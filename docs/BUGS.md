# Bug 记录 / Bug log

持续监测期间的发现，按时间倒序。**只有在能复现或被日志/实测证据支撑时才记进来**；
每条都写明「现象 → 根因 → 修法 → 怎么验的」。

状态：`已修` / `待修` / `不是 bug（实测澄清）`

---

## v1.7.x — 逐条校对（2026-09-12 追加）

> 这一轮的教训：**「有值」和「能用」之间隔着一整类错误** —— 占位符被吃掉、哨兵残留、
> 源串本身有歧义、术语表把复合词切断。这些都不会报错、不会白屏，只是安静地变难看。
> 所以新增 `tools/i18n-proofread.mjs`：把 565 个词条 × 25 个语言的**显示值**逐条与源串比对，
> 结构性破坏直接判失败（棘轮），可疑项记账。它一次就抓出了下面 48~52 全部五条。

| # | 现象 | 根因 | 状态 |
| --- | --- | --- | --- |
| 54 | 数字 + 量词的读法在俄语/乌克兰语/波兰语/塞尔维亚语/阿拉伯语里不合语法（`21 элементов` 应为 `21 элемент`） | 界面是 `${n} ${t('items')}` 这样拼接的，而 `items` 只有**一个**形式。这些语言的量词要按数词变格（one/few/many/other 四类），拼接式标签做不到。之所以一直没暴露：中文和英文都只需要一种形式，而我是拿中文写的文案 | **待修**（修法：`t(key, {n})` 支持数词参数 + 每种语言一张复数分类表（`Intl.PluralRules` 正好能做），先把 `items` 这类高频标签改掉。现状已写进 `docs/DESIGN.md`，不假装看不见） |
| 53 | 巡检里「基线已建立」这条断言**随机红**（同一份代码，前一次 5 个标记、下一次 2 个） | 它点完「全部检查一次」就**死等 6 秒**再断言 —— 而这个检查要联网（B 站那条目标），网慢一点就还没写完。固定等待在这种地方等于运气测试（这个项目已经踩过：BUGS #37 那条也是从「死等 1.2 秒」改过来的） | **已修**（改成**轮询到出现为止**（每秒读一次页面，最多 25 秒），失败时仍然给出标记数。同一类改动以后遇到就照着做：等「状态变化」，不等「时间过去」） |

| # | 现象 | 根因 | 状态 |
| --- | --- | --- | --- |
| 52 | 提示句里的 `**粗体**` 在界面上是**一串星号**（实测：`.hint` 里 0 个 `<strong>`、字面 3 处 `**`） | 提示句是当**纯文本**渲染的（`<div className="hint">{t('x')}</div>`），而词条里写的是 Markdown 强调。同一件事在英文里被写成了大写（`your BROWSER`）、在中文里写成 `**浏览器**` —— 两套强调方式，界面只认其中一套（大写），另一套显示成星号 | **已修**（`markdown.jsx` 导出**行内**渲染 `Inline`（不产生块级元素），5 个词条的 6 处提示改用它；英文那三处大写改回 `**…**` 与源串一致。自检：`i18n-proofread` 的「加粗标记不配对」把这种漂移卡住） |
| 51 | 葡语界面出现「daqui a ⟦0⟧ dias」—— 使用者看到的是**哨兵** | 源串「天后」里**没有**占位符（tokens 为空），模型却自己写了个 ⟦0⟧ 出来（它在提示词里见过这个东西），而 `restore()` 只检查「我发出的哨兵有没有丢」，**不检查译文里有没有多出来的哨兵** → 直接写进机器层。四个语言中招 | **已修**（新增 `hasStraySentinel()` / `looksBroken()`：还原后出现任何 ⟦n⟧ 都不合格；不合格就不写入、旧值同样是坏的则删掉（界面回落英文）；覆盖度的判据也换成 `looksBroken`，标签从「疑似未翻译」改成「坏译文」。自检：`--mock-mode sentinel` 端到端验证「补译 → 仍残留 → 不写入」） |
| 50 | 「N 天后」在四个语言里是错的：`Diva` / `Королева` / `ملكة البوب`（**歌后**） | 源串「天后」单独看有歧义（既可读作「N 天之后」，也可读作「歌后」），而且它是**后缀式**标签（界面拼成 `${d} 天后`）—— 别的语言里这个成分常在数字**前面**（pt「daqui a 3 dias」/ ru「через 3 дня」/ ar「بعد 3 أيام」）。模型既翻错了词，又被拼接方式逼出病句 | **已修**（源串改成带占位符的**整句** `{n} 天后`，`Calendar.jsx` 用 `replace('{n}', d)`；`{n}` 会被管线当占位符保护，模型动不了它。14 个语言重译后位置全对：`Dans {n} jours` / `Через {n} дн.` / `بعد {n} يوم`） |
| 49 | 十个语言的直播页标题成了「Iniciar emisión **Monitoreo** y…」「Début de diffusion **Surveillance** et…」—— 像把「开始直播」和「监测」各翻了一遍拼起来 | 术语表里「监测」是受保护术语，而源串「**开播监测**与多屏观看」里它和「开播」粘在一起成复合词：保护后模型看到的是「开播⟦0⟧与…」，于是把「开播」当**动词**翻（start streaming），再拼上哨兵值 → 病句。术语保护越严，切断复合词时越危险 | **已修**（把「开播监测」作为**复合术语**补进术语表，给 12 个语言写了约定叫法；顺带给管线加 `--keys` 支持**定点重译**（「一个键在十个语言里都坏了」不必重跑整本）。重译后：`Monitoreo de emisiones y visualización multipantalla` / `Surveillance des diffusions et visionnage multi-écrans`） |
| 48 | 英文字典与简体源串**结构**漂移：`tempDirHint` 丢了 `%TEMP%`（那段正是在讲 Windows 默认写在哪）、三处用大写代替 `**…**`、`searchHint` 多出一处粗体 | 英文是最早手写的，之后中文提示改了、英文没跟着改。校对器逐条比结构一眼看穿 —— 这类漂移在界面上只是「读起来少了一点点」，没人会去查 | **已修**（英文补齐 `%TEMP%` 与粗体标记；`searchHint` 反过来把强调加进**中文源串**再重译 14 个语言，让强调在所有语言里一致。自检：校对器的占位符/加粗检查把它卡住） |

---

## v1.7.x — 翻译层与覆盖度口径的审计（2026-09-12）

> 这一轮的共同点：**表格全绿、界面漏字**。四条都是「度量与实现各写一份」导致的漂移，
> 所以修法都是「把同一件事收敛到唯一一份实现」，并给每条补上会失败的断言。

| # | 现象 | 根因 | 状态 |
| --- | --- | --- | --- |
| 47 | 点开「直播」页之后，**整个控制台卡住约 4 秒**：再点「设置」只看到「加载中…」，别的页面也一起停 | 直播页一挂载就请求 `/api/accounts`，而它直接调 `listAccounts()` —— **同步** SQLite + `execFileSync` 解 DPAPI，实测 3.8 秒，这期间 Node 的单线程事件循环是停的（服务端日志里 `/api/live?fresh=1` 也跟着变成 3755ms）。和 BUGS #37 是同一类：不是「这个接口慢」，是**一个接口把服务冻住**。之所以现在才暴露：巡检原先打开直播页时并没有紧接着去读别的页面 —— 是我这轮加的「韩语直播页不能有中文」这条检查顺手点了直播页、又马上点设置，才把它照出来 | **已修**（两点：① `/api/accounts` 改走 `getAccounts()` 的 60 秒缓存（对外发声前仍然是强制现读）；② **直播页挂载时不再读登录态** —— 它读的是浏览器 cookie 库，使用者没打算发弹幕时本来就不该碰（隐私闸门），改成点「检查登录态」才读。实测：该接口调用消失，`/api/live?fresh=1` 3755ms → 303ms，`/api/state` 全程 17ms） |
| 46 | `machine.json` 里堆着一批**永远不会显示**的机器词条，其中带汉字的还会被「疑似未翻译」抓出来 | 两种死条目：① 人工层已写过的键（人工压过机器）；② 原文即英文的键。它们既不影响界面，也永远轮不到重译，却一直挂在统计里 —— 让人去修一条界面上不存在的东西。清掉 ko-KR 那条坏译文就是靠这条剪枝完成的 | **已修**（写盘前 `pruneMachine()` 统一剪掉，并打印剪了多少条；覆盖度统计也只算**没被人图层压住**的机器词条） |
| 45 | ko-KR 界面里出现 6 条**中韩混排**（例如「已加入모니터링」「开播是最有时效性的정보 —— …」），而覆盖度显示 100% | 模型在温度 0 下对同一输入**确定性**地返回中文原文（只把术语表的词换成韩语）；于是「重译」等于把同一份坏译文再要一遍 —— 两轮 `--bust suspicious` 之后仍是这 6 条。我一开始只改了提示词，但输入与温度都没变，等于没变 | **已修**（① 多一步**任务不同**的补译：把那份半成品当输入，让它「翻完」，5/6 条一次修好；② 其余仍不合格的**不写入**机器层，界面回落英文 —— 英文没翻译至少不冒犯任何人，写中文才是；③ 旧值同样是坏的则一并删掉，否则「宁缺勿坏」只是句口号；④ 剩下那条长句 `liveHint` 由**人工层**接管 —— 人工永远压过机器，长提示句本来就该人工过一遍。自检：`--mock-mode echo` 端到端验证「补译 → 仍不合格 → 不写入」） |
| 44 | 「疑似未翻译」报了 37 条，逐条看下来**24 条是假警报**（`泠鸢` / `嘉然今天吃什么` / `萌百` / `盯日箱` / `通义` 都是政策上要保留的专有名词） | 剔除保留词时**先剔短后剔长**：术语表里同时有「嘉然」和「嘉然今天吃什么」，先剔「嘉然」会把长词切成「今天吃什么」，于是合法保留的专有名词反而被当成漏译。判据还在两个文件里各写了一份 | **已修**（按长度**从长到短**剔除；术语表补齐这 5 个保留词，让政策变成可机器核对的规则；覆盖度不再自己复制一份判据，改为直接调用管线里的那一个函数。37 → 13 条，且 13 条全是真漏译） |
| 43 | 每种语言都差**同样 2 条**（563/565），永远补不齐 | 那 2 条是 `appTitle`（产品名）与另一条原文即英文的词条 —— **本来就该显示英文**，却被算进「需要本地化」的分母。度量口径错了，看起来就像漏译 | **已修**（新增「语言中立」判据：简中原文与英文**逐字相同**的键不进分母、不浪费翻译调用、写盘时剪掉。判据取自源码本身，不另立豁免清单 —— 清单会漂移。分母 565 → 560，25 种语言全部 100%） |
| 42 | 「检查登录态」那段长提示（`loginHint`）在**所有**语言里都没翻译；`appTitle` 同样 | `tools/lib/i18n-source.mjs` 读词条时只认「同一行的单引号字符串」。`appTitle: "Vtuber's Monitor Link"` 是双引号、`loginHint` 是换行后 `'甲' + '乙' + '丙'` 跨行拼接 —— 两种都被读成**空串**，而空串在管线里等于「这条不用翻」，于是静默跳过。繁体没暴露是因为它由 OpenCC 走另一条路径整份生成 | **已修**（四种写法全认：同行单/双引号、换行取值、跨行 `+` 拼接、嵌套对象记空串且不吃掉后面的行；新增自检断言四种形态 + 「嵌套对象不能吃掉后面的行」+「注释里的引号不算值」） |
| 41 | `pt-PT` 有 4 条短词条（`probe` / `tab_live` / `tab_search` / `tab_settings`）**永远拿不到机翻**，界面一直是英文兜底 —— 而覆盖度报 100% | `humanKeys()` 把「同 base 的**所有**地区」都算成人工已有，于是 pt-BR 写过的词条挡掉了 pt-PT 的机翻。但 pt-PT 的 chain 是 `['pt-PT','en-US']`，运行时**根本不会**继承 pt-BR。同一个「回落链」规则在三个文件里各写一份，两份一致、第三份漂移 | **已修**（新增 `tools/lib/locale-chain.mjs` 作为工具侧唯一实现，覆盖度与翻译管线都用它；自检直接断言链：`pt-PT` 不继承 `pt-BR`、`es-MX` 继承 `es-ES/es-419`、`uk-UA` 不继承 `ru-RU`。补完这 4 条后 pt-PT 达到 560/560） |

---

## v1.7.x — 设置区审计与实时监测（2026-09-11）

| # | 现象 | 根因 | 状态 |
| --- | --- | --- | --- |
| 40 | 弹幕功能一直卡在 `-352`（风控），带着登录态也一样 —— 一度以为是「B 站要求登录 / 要求代理」 | 真正缺的是 **WBI 签名**。B 站对 `getDanmuInfo` 这类接口加了 `w_rid`/`wts` 校验，**有 cookie 并不够**。之前的探索方向（换代理、换账号、带 cookie）全都跑偏了，因为 -352 看起来像风控、不像「参数不全」 | **已修**（新增 `server/src/wbi.js`：nav 取 key（**匿名即可**）→ 64 位置换表取前 32 位得 mixin key → 参数按名排序、过滤 `!'()*`、拼 mixin 后 md5 = `w_rid`；遇 -352/-403/-412 强制换 key 重试一次。已接入弹幕发送与 `feed/space`。**实测**：签名的 `getDanmuInfo` 从 -352 变成 `code=0 message=OK`；写接口变成 `-101 账号未登录`（说明签名这关过了，剩下确实只是登录态）。自检 `tools/wbi-test.mjs` 14 项离线 + `--live` 联网验证） |
| 39 | 说「界面支持 12 种语言」，但**真实覆盖只有 4~5%**（562 条里 25 条），其余全靠英文兜底 —— 而没有任何地方会报错 | 「缺键回落英文」是正确设计，但缺了「覆盖度」这个度量，于是「加了语言」和「界面真的是那个语言」被混为一谈；不加度量也没法防止后续加功能时悄悄退化 | **已修**（新增 `tools/locale-coverage.mjs`：只统计该语言**自己提供**的键（英文兜底不算），结果写成 `locales/coverage.json` 基线并在 `verify` 里卡下限 —— **棘轮**：覆盖度只能往上走。同时给 9 个主要语言各补了 40 个高可见度词条，覆盖率 4~5% → 6~8%） |
| 38 | `uk-UA` / `pl-PL` / `sr-RS` 的界面里出现**俄语**文案 | 它们的回落链里写了 `ru-RU`（当初只想「缺键时有个兜底」），于是俄语词条被当成自己的文案显示 —— 把俄语当乌克兰语不是「翻译不够好」，是明确的错误 | **已修**（回落**只在同一语言内**继承地区差异：`es-MX→es-419→es-ES`、`fr-CA→fr-FR`、`pt-BR→pt-PT`、`zh-TW→zh-Hant→zh-Hans`、`en-AU→en-GB→en-US` 都对；跨语言一律走**英文**兜底。覆盖度表也因此从虚高的 44 掉到真实的 21/21/17） |
| 37 | 打开「报告」页后报告列表**迟迟停在「加载中」**，几秒才对；分享目标表也是空的 | `/api/share/targets` 每次都调 `listAccounts` 现读登录态 —— 那是**同步阻塞**操作（同步 SQLite + `execFileSync` 调 PowerShell 解 DPAPI），把 Node 的单线程事件循环整个堵住，连累所有其它请求。不是「页面慢」，是**一个接口拖住整个服务** | **已修**（读登录态加 60 秒缓存：开页面走缓存，**对外发声前强制重读** —— 拿过期的登录态判断去发帖等于用旧钥匙开新锁。巡检那条断言也从「死等 1.2 秒」改成「等列表真出现」，固定等待会变成运气测试） |
| 36 | 生成分享包 500：`ERR_INVALID_CHAR: Invalid character in header content ["content-disposition"]` | 分享标题往往是中文，而 `Content-Disposition` 里带了中文文件名 —— **HTTP 头只能是 ASCII**。单元测试只验证了 `bundleFilename` 生成得对，**没走 HTTP 层**，所以没抓到；巡检的真实请求抓到了 | **已修**（按 RFC 5987/6266：`filename="<ASCII 兜底>"; filename*=UTF-8''<百分号编码>`，现代浏览器优先用后者，中文名保住。自检加了一条「头必须是纯 ASCII」的断言 —— 这种错只有跨过 HTTP 层才会暴露） |
| 35 | 来源健康度的「平均耗时」算出来比任何一次实测都大（460/3=153，实测是 100/110/120） | `ms_sum` 把**失败那次**的耗时也累加了，而除法用的是成功次数。同一批自检里还有一个**我自己的断言错误**：按来源分组的序列是**稀疏**的（只含真有数据的日子），我却断言它等于 30 天 | **已修**（只累加成功那几次的耗时 —— 平均耗时的语义是「成功的时候有多快」；测试改为断言稀疏语义，并在函数注释里写明） |
| 34 | 1000 条归档写入要 3.6 秒 | 逐条 `INSERT` 各自一个隐式事务（1000 次 fsync） | **已修**（整批包一个事务 + 失败整体回滚。**3.6 秒 → 12 毫秒**，快 300 倍。写批量写入时这该成为条件反射） |
| 33 | 同一批 4 次来源健康检查只留下 1 条 | `source_health` 把 `at`（时间戳）放进了主键 —— 同一毫秒内的多次写入被 `OR REPLACE` 覆盖。健康度天然是**按天聚合**的统计，用时间戳当键从根上就错了 | **已修**（主键改为 `(day, source_id)`，写入改为累加 `checks/ok/ms_sum`，查询直接算成功率与均值。「4 次写入应得 checks=4」那条断言就是抓它的） |
| 31 | 巡检验证「重复报道会被合并」一直失败（去掉 0 条） | 我的断言假设 mock 语料是 20 条重复内容，**其实那 20 条各不相同** —— 断言依赖了不成立的前提。差一点就把它改成「永远通过」，那样等于把这条检查删掉 | **已修**（新增 `POST /api/events/preview`：拿一段条目试合并效果、不动历史。巡检用它注入「3 个来源的同一件事 + 1 条无关」，端到端验证 2 个事件 / 去掉 2 条重复 / lead 是权重最高的来源。这个接口对使用者也真有价值 —— 可以自己调阈值看效果） |
| 30 | 合并闸门两头都漏：① 20 条**内容完全相同**的条目一条都没合并；② 加上「相似度≥0.8 就放行」后，60 条**只差一个编号**的条目又被链成一个簇 | ① 「必须共享罕见词」的闸门在内容完全相同时必然失效 —— 每个词的 df 都等于文档数，罕见词一个不剩；② 光靠相似度区分不了「同一件事的重复报道」和「只差编号的 60 条不同帖子」 | **已修**（闸门改为「共享罕见词 **或**（相似度≥0.8 且数字/日期兼容）」。数字兼容这条是关键：真重复的日期当然一致，只差编号的不是同一件事。两条都写进了自检） |
| 29 | 三个来源的同一件事只并成两个簇（i1~i3 像、i2~i3 像，但 i1~i2 不像） | 我把单链接实现成「只并入**已分配**簇的邻居」，于是先出现的条目反而落单 —— 桥接关系断掉 | **已修**（改成并查集做真正的单链接：先把够像的两两连边，再取连通分量。顺带彻底摆脱「结果依赖输入顺序」—— 自检里那条断言就是抓它的） |
| 25 | 发布流水线第一次运行**拒绝发布**，报了 13 处 | 多数是「用发行版校验器去扫源码树」造成的误判（源码树当然没有 exe / app/server/node_modules）；另有 2 处是**真问题**：① `package.json` 里有个 em dash（非 ASCII，而它是必须 ASCII 的关键文件）；② 我自己写的发布脚本里**把输出目录写成了本机的绝对盘符路径** —— 无痕化要抓的就是这种东西（而且它真的被自己的扫描器抓到了） | **已修**（给校验器加正经的 `--scan-only` 模式，而不是在调用方过滤它的输出 —— 过滤校验器的输出等于把它嘴捂住；`package.json` 改纯 ASCII；输出目录改用 `VML_RELEASE_OUT` 或工程同级推导，源码里不留任何本机路径） |
| 24 | CI 工作流与本机发布**不同源** | 早期工作流用 `Compress-Archive dist/VtuberMonitorLink` 整目录压 —— 既没有清单、也就**跳过了「包里没有运行期数据（API Key / 历史报告）」这条断言**；`npm ci` 没带 `--ignore-scripts`，会让 playwright 在 CI 上拉 ~150MB 浏览器内核（便携 exe 根本不需要）；而且它在构建**之前**就跑发行版校验（那时 dist 还不存在） | **已修**（工作流改为与本机同源：`verify:fast` → `build-portable`（zip 与清单都由它产出）→ 完整 `verify` → 生成 SHA256SUMS → 附到 Release） |
| 23 | 完整性检查报「界面用到不存在的词条 `tab_live` / `liveCheck`」等 10 条，但界面显示是对的 | 我用编辑工具插词条时有 12 次 `old_string` 以换行结尾而替换内容没有，于是**两个键被粘到同一行**（`llmFeat_probe: '…',    tab_live: '直播',`）。键都在、界面正常，但按行首匹配的脚本只看得见第一个。检查器自己也有两个 bug：硬要求 4 空格缩进（拆行后是 6 空格）、`en` 块一路取到文件末尾（把 `fmtDate`/`en-GB` 这些运行期字段当成词条键） | **已修**（新增 `tools/format-i18n.mjs`；词条键改为**忽略字符串后按花括号深度**提取。并补上真正缺的 4 个键 `language`/`name`/`date`/`detecting` —— 之前界面会显示字面量 `name`） |
| 22 | 推送自检里两条诡异失败：① 「首次去重判断就返回重复」；② 「静默期内不该投递却投递了」，且队列莫名是空的 | ① 我把测试目录写成 `cfg.logsDir`，而 `resolveDir` 读的是 **`cfg.paths.logsDir`** —— 测试于是在**静默地读写仓库里真实的 logs 目录**，上一次运行留下的去重记录与队列污染了这一次；② 我把 `"23"`（只有小时）当 `"HH:MM"` 传给 `toMinutes`，被判非法 → 走 fail-open 直接推送（产品行为是对的，测试错了） | **已修**（目录放进 `cfg.paths`；断言里先验证 `inQuietHours` 判定为 `true` 再测投递。并把 **fail-open 写成断言**：配置写坏时照常推送，因为「配错导致所有通知消失」比「偶尔半夜响一下」严重得多） |
| 21 | 静默期内的通知偶尔**立刻发出去**，有时又正常入队 | 我在 `notify()` 开头加了 `flushQueue(cfg, log).catch(() => {})` —— **不 await**。它会在调用方已经改过配置之后才真正执行，而静默判断用的是「执行那一刻」的配置，于是刚入队的通知被立刻补发 | **已修**（投递路径里绝不做补发。补发只由 runner 每次运行结束时 `await flushQueue(...)`、或界面「立刻补发」触发。这是「副作用放错位置」而不是缺锁） |
| 20 | 巡检自己制造的两个假失败：① 「日报里没有倒计时区块」其实读的是巡检自己写的 `legacy-sample.md`；② 另一版又把日期写死成 1 月 15 日 | ① 报告列表按**文件名**排序，`legacy-` 在字典序上排在日期前面 → 改成按 mtime 取最新（和之前 `hasText` 挑行的坑同一类）；② 日报只列未来 30 天内，写死日期换个日子跑必然失败 → 改成动态算「今天 +5 天」 | **已修**（两条都在巡检里，且有断言输出说明取到了哪个文件） |
| 19 | 日历页的时区显示为空（`2026-09-12 @`），但计算是对的 | `cfg.calendar.timeZone` 默认是**空字符串**，而我写的是 `opts.timeZone ?? cfg?.calendar?.timeZone ?? undefined` —— `??` 只认 null/undefined，空串会一路传下去 | **已修**（改用 `||`：空串按「未配置」处理 → 返回系统时区。断言里现在会打印 `今天 @ 时区`） |
| 18 | 新增纪念日接口 500：`setConfig is not defined` | 我写的辅助函数放在**模块顶层**，而 `getConfig/setConfig/onConfigChanged` 是 `createApp` 的**参数** —— 只有工厂内部看得见。更早一版还错写成根本不存在的 `saveConfig`。`node --check` 对这两类完全无感 | **已修**（`patchConfig` 移进工厂内部；并且巡检里加了一条**真的写配置**的断言 —— 这类错误只有打到接口才会暴露，光有语法检查等于没查） |
| 17 | 繁体界面出现「部分繁体 + 部分简体」混排（`情報 / 檢索 / 运行 / 监视`） | 简→繁靠**手写对照表**，而「简繁同形字」与「我漏掉的字」在表里无法区分：`运`/`监` 漏了 → 它们原样留在繁体界面里。586 个汉字里只有 203 个有映射 | **已修**（删掉整张手写表，改为构建期用 **OpenCC 词典**整份生成 `locales/generated.js`：`t`/`hk`/`twp` 三变体。生成器带三道自检：① ASCII/占位符结构不许被改；② 差异率异常低＝词典没加载；③ 单字复查「疑似漏转简体字」，未复核的让构建失败。`npm run verify` 会校验词条是否最新） |
| 16 | 「自检」把命中的 API Key 原文打印到了终端 | `verify-release.cjs` 报告问题时回显了匹配到的整行 | **已修**（`redact()`：只报文件/行号/长度，输出 `"sk-…<35 chars redacted>"`。**自检的第一职责是不制造泄漏**） |
| 15 | 巡检断言「点开 .html 报告应出现 iframe 预览」总是失败，且点到的其实是 `.md` 那一行 | 用 Playwright `hasText: '.html'` 挑表格行 —— **每行「对比」下拉里都列着别的报告名**，于是 `.html` 的文件名在 `.md` 那一行的文本里也匹配上了 | **已修**（改成先取 `button.link` 的文本列表再按 `/\.html$/` 选名字；断言输出里带上 `clicked=… opened=…`，下次一眼就能看出挑错了行） |
| 14 | `npm run build` 只跑了 vite，没出 exe；直接跑 `node npm-cli.js` 报 `Cannot find module` | `build` 脚本 = `vite build`，打包是独立的 `tools/build-portable.cjs`；而 `npm-cli.js` 不在仓库里（在 Node 安装目录下） | **已修**（打 exe 走 `node tools\build-portable.cjs`；这条写进 RELEASE.md 的流程） |
| 13 | 打包时 `EBUSY: resource busy or locked, copyfile …VtuberMonitorLink.exe` | 正在运行的 app 锁住了 exe | **已修**（先停 app 再打包；脚本给出可操作提示，不再拿过滤后的输出把错误吞掉） |
| 12 | `GET /api/client-log` 返回 500 | 新加的路由用了 `resolveDir` 但没 import（`server.js` 只 import 了 `APP_ROOT`） | **已修**（补 import；JSON 错误兜底把它变成 500 而不是崩进程） |
| 11 | 设置页整个白屏 | 我在 `if (!cfg) return` **之后**加了 `useRef` → 首帧/后续帧 hook 数不一致 → React #310 卸掉整棵树。**同一类错误第二次犯** | **已修**（hook 全部提到早退之前；文件里注释写明踩过两次） |
| 10 | 每次保存配置都排一次补跑；新建的任务立刻被判为「错过」并马上跑 | 补跑判断只看「上次执行 < 上一个应触发时间」：① 没历史 = 从未执行 ⇒ 一律当成错过；② `scheduler.start()` 在 `onConfigChanged` 里，每存一次配置就重排一次 | **已修**（三道闸门：必须跑过至少一次 / 同「任务+时间点」只补一次 / 只补 7 天内。验证：新任务 0 次、真错过 1 次、再存 10 次 0 次） |
| 9 | 任务名输入框每敲一个字发一次 `PUT /api/config`，每次都会把整份配置写盘 | `onChange` 直接落库，没有防抖 | **已修**（本地改 + 停输 800ms/失焦再存。验证：10 字 → 1 次 PUT，修复前 10 次） |
| 8 | 有输入框却不显示（设置页只有一句「请先填入 API Key」） | 那些字段包在 `{active && (...)}` 里，档位数组为空时 `active` 为 null | **不是 bug，但状态不好**（已用 API 预建 DeepSeek 档位） |
| 7 | 设置页某个 `select` 点不动 | 那是「启用代理」，出口已设为 Tor，该字段被**正确禁用** | **不是 bug**（联动是对的） |
| 6 | 任务行的「模式 / 频率」select 没有 label | 表格单元格里没放 `.field` 包裹 | **已修**（表头不是 label，读屏读到的是一个孤立的 combobox。给任务行 7 个控件都补了 `aria-label`，带上任务名做区分；`section.tasks` 的「新增任务」按钮也加了 `.add-task` 钩子，巡检不再按文案选按钮。验证：traverse-ui 断言 `7/7`） |

## v1.6.0 — 用一个新遍历去覆盖「从没跑过的流程」

新增 `tools/traverse-flows.cjs`：把打包 exe + mock LLM + **一个真的本地 webhook 接收器**
一起跑起来，覆盖告警推送实际投递、配置导入导出、情报标记、计划任务执行、LLM 助手。
三个 bug 全是它抓到的：

| # | 现象 | 根因 | 状态 |
| --- | --- | --- | --- |
| 5 | 导入「脱敏导出」的配置会**抹掉已存的 LLM Key** | 合并逻辑对数组是**整体替换**，绕过了「空字符串不覆盖已有非空值」的保护 | **已修**（按 id 合并对象数组） |
| 4 | webhook 地址没打码，`/hook` 这种短路径原样漏出 | 掩码规则只处理 ≥6 字符的末段；而 Discord/飞书的密钥**就在路径里** | **已修**（整条路径与查询串都抹掉） |
| 3 | 给条目打自定义标签**写不进去** | `PATCH /api/intel/:id` 只接受 `starred/read/note`，`tags` 被静默丢弃 | **已修**（接受并清洗，最多 20 个、每个 40 字） |

## v1.5.0 — 特征抽取与 Tor

| # | 现象 | 根因 | 状态 |
| --- | --- | --- | --- |
| 2 | 应用进程直接退出、网页全白 | `features.js` 的 prompt 模板里写了 `${startIndex + n}`，`n` 不存在 → `ReferenceError`；**Express 4 不捕获 async 路由抛错** → 未处理拒绝带走进程 | **已修**（修变量 + `unhandledRejection`/`uncaughtException` 兜底 + JSON 错误中间件；并让遍历捕获被测应用的 stdout/stderr —— 正是这条改动找到了根因） |
| 1 | 报告页一个标题都没有、导出只有 1.3KB | mock LLM 用 `/共\s*(\d+)\s*条/` 判断是不是抽取请求，而**报告 prompt 里也有这句话** → 报告被换成了 JSON 数组 | **已修**（只按 system prompt 区分两条路径） |

## v1.4.0 — 本地检索

| # | 现象 | 根因 | 状态 |
| --- | --- | --- | --- |
| — | 打包后的 exe 起不来（`Invalid or unexpected token`） | `3D披露` / `2434` 作为对象键**没加引号** —— 以数字开头的键不是合法标识符，整个模块加载失败。打包脚本只看 `--doctor`，那时还没加载到出错的模块，完全没拦住 | **已修**（加引号 + `verify` 新增「随包每个 .js/.cjs/.mjs 跑 node --check」） |
| — | B 站免登录图文动态一设时间区间就被整批排除 | `opus/feed/space` 的 `pub_time` 为空，条目没有时间戳 | **已修**（回落到「首次见于哪次运行」并标 `tsSource`） |
| — | 内容类型是 HTML 却让前端 `JSON.parse` | 路由抛错时 Express 默认回 HTML 错误页 | **已修**（JSON 错误兜底） |

---

## 持续监测的机制（怎么看见问题的）

三层，缺一层就会有盲区：

1. **服务端请求日志** —— `createApp` 里的中间件记 `METHOD path -> status (ms)`，
   写操作（POST/PUT/PATCH/DELETE）标「写入」，≥400 走 warn。排除了 3 秒一次的 `/api/state` 轮询。
2. **客户端错误信标** —— `web/index.html` 里在**任何模块加载之前**挂 `error` /
   `unhandledrejection` / `console.error` 监听，发到 `POST /api/client-log`，
   落进服务端日志与 `logs/client-errors.jsonl`。同一个错误只报一次，避免刷屏。
   **没有它，页面崩了服务端一无所知** —— 这一条是被 #11 逼出来的。
3. **行为取证** —— `logs/` 下还有 `danmaku.jsonl`（发弹幕审计）、
   `schedule-history.jsonl`（计划任务执行）、`live-state.json`（开播状态基线）。

## 追加：打包/调试流程上的坑（2026-09-12）

> 编号接主表继续（原先是 13/14，与主表撞号了 —— 主表已经排到 25）。

| # | 现象 | 根因 | 状态 |
| --- | --- | --- | --- |
| 28 | 「按人关注」的匹配在生产数据上**等于没生效**：来源名这条最强的归属信号完全不起作用 | 条目里的 `sourceName` 是本地化对象 `{ zh, en }`，而匹配器对它做了 `String(value)` → `"[object Object]"`，于是永远匹配不到任何真实别名。**不报错、不警告**，只是安静地一条都不归属。之所以被发现，是因为我把命中的别名打进了断言输出，看见了 `[object Object]` 这个荒谬的值 | **已修**（新增 `fieldStrings()`：字符串/数字/数组/对象一律摊平成候选字符串再匹配；并补了「本地化对象字段要能匹配」的断言。顺带把 `sourceName` 提进匹配字段 —— 来源名本身就是人名时，这个来源就是那个人的账号，这是最强信号） |
| 27 | 反复出现「改了代码但产物没变」：`app/web/dist` 里还是旧的哈希，遍历断言一直失败 | **运行中的 app 锁住了 `dist\...\VtuberMonitorLink.exe`**，`build-portable` 复制 exe 时抛 `EBUSY` 直接中止 —— 后面的「收集 app/」这一步根本没执行，所以前端产物没更新。而我用 `Select-String` 只筛了几行，把失败信息全过滤掉了，看起来像「构建成功」 | **已修**（两点：① 构建脚本捕获 EBUSY 并给出人话提示「exe 正在运行，先关掉或用 --out 换目录」；② 我自己的流程改成**先停 app 再打包**。教训：**过滤构建输出时必须保留错误**） |
| 26 | LLM/API 填写入的地方「找不到」 | 它埋在「设置」里，且字段包在 `{active && ...}` 里 —— 档位数组为空时连输入框都不渲染，只剩一句「请先填入 API Key」 | **已修**（单独拉出独立页面 `pages/Llm.jsx`，并且没有档位时给一个明确的「建一个档位」按钮，不再是死路） |
| 32 | 巡检验证「按人归属」时 0/20 命中，打印出的样本还是空的 | 我又把 `??` 用在可能为**空字符串**的字段上（`title ?? text`），空串不会继续往下取 → 样本为空、别名取自错误字段。**这和 #19 的 `??`/空串是同一个坑的第二次** | **已修**（统一改用 `||`；别名改为取自条目里真实存在的文本，别名选错就等于没验证） |
